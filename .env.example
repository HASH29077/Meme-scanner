# Copy this file to .env.local for local dev (vercel dev), and add the
# same two variables in Vercel -> Project Settings -> Environment Variables
# for the deployed site. Never commit real values.

BIRDEYE_API_KEY=
HELIUS_API_KEY=
